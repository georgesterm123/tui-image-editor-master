{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1",
    "prettier": "^2.0.5"
  },
  "scripts": {
    "build": "lerna run build",
    "build:image-editor": "lerna run --scope tui-image-editor bundle"
  }
}
